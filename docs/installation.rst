============
Installation
============

At the command line::

    $ easy_install django-usage

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-usage
    $ pip install django-usage
